const id = document.querySelector("#heading")
// console.log(id);
// console.dir(id)

const classes = document.querySelectorAll(".para")
console.log(classes);
console.log('Using for loop');

for (let each of classes) {
    console.log(each)
}

const elements = document.querySelectorAll("a")
console.log(elements);

